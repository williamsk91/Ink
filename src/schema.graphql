type Error {
  path: String!
  message: String!
}

type User {
  id: ID!
  email: String!
}

type Query {
  me: User
}

type Mutation {
  register(email: String!, password: String!): [Error!]
  login(email: String!, password: String!): [Error!]
  logout: Boolean
}
